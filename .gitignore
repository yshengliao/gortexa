# gen/ is committed: `go mod tidy` in consumer projects resolves the test
# dependencies of imported packages, and those tests import gen/resource/v1 —
# a published module missing gen/ breaks every consumer's tidy. Still never
# hand-edited: produced only by `make gen`; CI fails on drift.

# Build output
/bin/
*.out
coverage.*

# Local env / secrets
.env
*.local.yaml

# Editor / OS
.DS_Store
